class Solution {
    public int[] sortedSquares(int[] nums) {
        
        int end = nums.length;
        int[] temp = new int[end];
        for (int i = 0; i<end; i++){
            temp[i]= nums[i] * nums[i];
    
        }
        Arrays.sort(temp);
        return temp;
    }
}
